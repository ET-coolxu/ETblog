---
description: 基础遵守的规则：先问清、中文注释、Git 与命令边界
alwaysApply: true
---

# 回复与实现前

- 细节不清时先问再答
- 写代码前在 Planning 模式穷尽追问，消除假设后再实现

## Git

- 不要执行 `git commit`、`git push` 或代为提交；改完代码后把变更说明给我，由我手动提交

## 命令

- 不要运行会花较长时间的命令（例如 `npm install`、`npx create-next-app`、`next build`、`docker compose`、长时间测试或安装）
- 把完整命令写给我，由我自己在终端执行，再把结果发回
- 短、只读、可立刻结束的命令（看文件、搜代码、`git status`）可以自己跑

## 注释

代码必须能读。新增或改动的**导出函数、工具函数**，以及安全 / 鉴权 / 缓存 / 路径 / 解析等非显而易见逻辑，都要写**中文注释**。不要整文件无注释地交代码。

- **导出函数、工具、类型**：用 JSDoc，写清做什么、关键约束或副作用（例如防穿越、草稿对访客不可见）。参数仅在类型看不出时写。
- **重要逻辑**：在分支旁用一行说明「为什么」，不要复述代码在干什么。
- **模块**：`lib/` 等非页面文件在顶部用一两句说明职责与边界。
- **不要写**：复述函数名、给显而易见的赋值/循环作注释、和代码不一致的过时注释。
- **改旧文件**：只给本次改动触及的符号补注释，不要顺便给整个仓库补注释。

```ts
/**
 * 把合法 slug 解析成 `content/posts/{slug}.md` 的绝对路径。
 * 非法字符或路径穿越时返回 null，禁止读写目录外的文件。
 */
function resolvePostFile(slug: string): string | null { /* ... */ }

// ❌ 把 slug 转成文件路径
function resolvePostFile(slug: string): string | null { /* ... */ }
```
